# Pinned to the versions this was developed and validated against. Later releases
# will very likely work; these are what the reported numbers came from.
mujoco>=3.1
torch>=2.2
lightgbm>=4.3
catboost>=1.2
numpy>=1.26
pandas>=2.2
scipy>=1.12
scikit-learn>=1.4
plotly>=5.20
kaleido>=0.2.1          # static PNG export for plotly; the figures need it
matplotlib>=3.8
joblib>=1.3
imageio>=2.34           # video output for the visualisation notebooks
imageio-ffmpeg>=0.4
jupyter>=1.0
